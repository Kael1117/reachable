#!/bin/sh

case " $* " in
  *" scan "*)
    cat "$(dirname "$0")/../scan.txt"
    exit 1
    ;;
  *" trace "*)
    cat "$(dirname "$0")/../trace.txt"
    exit 0
    ;;
  *)
    echo "unexpected demo node args: $*" >&2
    exit 1
    ;;
esac
